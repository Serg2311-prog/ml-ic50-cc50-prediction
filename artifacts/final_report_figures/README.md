# Final report figures

This directory is populated by running:

```bash
python3 generate_final_report_figures.py
```

The generator requires the dataset at:

```text
data/Данные_для_курсовой_Классическое_МО.xlsx
```

All generated plots use descriptor-only feature matrices and validate that target-like columns are excluded before model fitting.
